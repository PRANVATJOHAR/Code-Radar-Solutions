#include <stdio.h>
int main() {
  int num;
  printf("");
  scanf("%d", &num);
  if (num>0)
  print("Positive");
  else if (num<0)
  print("Negative");
  else
  print("Zero");

    return 0;
}